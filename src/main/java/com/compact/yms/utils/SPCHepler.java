package com.compact.yms.utils;

public class SPCHepler {
	
	/*----------------------------------------------
	 * CL 구하기 
	 * ---------------------------------------------
	 * 1) 측정단위(VALUE1~20)의 평균(X'), 범위(R'), 분산(V') 구하기
	 * 2) 전체(기간)의 평균(X')의 평균(X), 범위(R')의 평균(R), 분산(V')의 평균(V), 군내표준편차(POWER(분산,0.5)), 전체표준편차(STDEV) 구하기
	 * 3) CL = 평균의 평균
	 * 4) CPL = (X - SU) / (3 * 군내표준편차)
	 * 5) CPU = (SU - X) / (3 * 군내표준편차)
	 * 6) CPK = CPL/CPU 중 작은 수
	 */
	
	
}
